import Component from '@ember/component';

export default Component.extend({

    /**
     * Defines the model of the languages
     *
     * @property {object} languages
     * @default null
     */
    languages: null
    
});
