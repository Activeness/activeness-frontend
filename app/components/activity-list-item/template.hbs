<a {{action 'openActivityDetails' activity.id}} 
    class="activity d-flex justify-content-start {{if showAsList 'activity-tile flex-column' 'flex-row'}}"
>
    
    <div class="activity-date">
        <div class="activity-date-day">
            {{format-date activity.date day='2-digit'}}
        </div>
        <div class="activity-date-month">
            {{format-date activity.date month='long'}}
        </div>
    </div>
    <div class="activity-image" style="background-image: url({{activity.img}});"></div>
    <div class="activity-information">
        <div class="activity-title">
            {{activity.title}}
        </div>
        <div class="activity-organizer">
            {{format-date activity.date}}
            {{#if activity.organizer}} / {{activity.organizer}}{{/if}}
        </div>
        <div class="activity-categories">
            {{#if activity.categories.isPending}}
              {{t 'app.rest.loading.categories'}}
            {{else}}
              {{#each activity.categories as |category|}}
                <a class="btn btn-secondary" {{action 'searchByCategory' category}}>
                  {{t category.title}}
                </a>
              {{/each}}
            {{/if}}
        </div>
    </div>
    
    {{yield}}

</a>