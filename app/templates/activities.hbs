{{main-image}}

<section class="section-light">
    <div class="container">
        {{activities-list 
            activities=model.activities
            categories=model.categories
        }}
        {{outlet}}
    </div>
</section>
